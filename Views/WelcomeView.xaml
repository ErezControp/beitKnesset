<UserControl x:Class="BeitKnesetDisplay.Views.WelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="White">

        <StackPanel VerticalAlignment="Center"
                    HorizontalAlignment="Center">

            <Image Source="/Assets/controp_logo.png"
                   Width="300"
                   Margin="0,0,0,40"/>

            <TextBlock Text="ברוכים הבאים"
                       FontSize="64"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"/>

            <TextBlock Text="לבית הכנסת קונטרופ"
                       FontSize="54"
                       Foreground="#4F46E5"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"/>

            <TextBlock Text="Welcome to CONTROP Synagogue"
                       FontSize="28"
                       Margin="0,20,0,0"
                       HorizontalAlignment="Center"/>

        </StackPanel>

    </Grid>

</UserControl>